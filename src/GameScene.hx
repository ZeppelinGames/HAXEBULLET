import h2d.Scene;

class GameScene extends Scene {
    public function init() {
        this.scaleMode = ScaleMode.LetterBox(Game.WINDOW_WIDTH, Game.WINDOW_HEIGHT);
    }

    public function update(dt:Float) {

    }
}